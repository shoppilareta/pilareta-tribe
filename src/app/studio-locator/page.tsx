import { StudioLocatorClient } from '@/components/studio-locator';

export const metadata = {
  title: 'Studio Locator | Pilareta Tribe',
  description: 'Find Pilates studios near you. Search by location, filter by amenities, and discover your perfect practice space.',
};

export default function StudioLocatorPage() {
  return <StudioLocatorClient />;
}
